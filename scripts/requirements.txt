# Dependencias obrigatorias
numpy>=1.24
rasterio>=1.3
pyyaml>=6.0

# Dependencias para geracao de figuras PNG (nao necessarias para --cog-only)
matplotlib>=3.7

# cartopy: mapas com linhas de costa (opcional, mas recomendado)
# Instale via conda se pip falhar:
#   conda install -c conda-forge cartopy
# cartopy>=0.22
